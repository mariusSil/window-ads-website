# Base URL for the website (used for SEO metadata, sitemaps, etc.)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Optional: Analytics tracking ID
NEXT_PUBLIC_GA_ID=

# Optional: Additional SEO configuration
NEXT_PUBLIC_SITE_NAME=AI Website Template
NEXT_PUBLIC_DEFAULT_LOCALE=en

# Email Configuration (Optional - uses localhost if not provided)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@langu-remontas.com
BUSINESS_EMAIL=info@langu-remontas.com

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=2

# Spam Protection
HONEYPOT_FIELD_NAME=website
